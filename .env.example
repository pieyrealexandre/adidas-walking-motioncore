# Shared Supabase project (same as bball)
VITE_SUPABASE_URL=https://<project-ref>.supabase.co
VITE_SUPABASE_ANON_KEY=<anon-public-key>

# adiGen backend (Cloud Run europe-west1)
# For local dev with the backend running locally: http://localhost:8080
# For production: the Cloud Run service URL
VITE_BACKEND_URL=http://localhost:8080
